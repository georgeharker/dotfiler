# Ties this repo to a crib memory project. `crib import` pulls the docs below
# into the `dotfiler` project (one-way: source wins, ids/history preserved).
project: dotfiler
import:
  - README.md
  - index.md
  - docs/**/*.md
